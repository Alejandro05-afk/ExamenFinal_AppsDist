events { }
http {
    upstream backPrueba {
        server servidor1:5000;
        server servidor2:5000;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://backPrueba;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}